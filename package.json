{
  "name": "@tertium/countries",
  "version": "0.2.0",
  "description": "Collection of countries with ISO codes.",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc && cp -r src/images dist/images",
    "test": "jest",
    "test:watch": "jest --watch",
    "version": "conventional-changelog -p angular -i CHANGELOG.md -s && git add CHANGELOG.md"
  },
  "keywords": [
    "country",
    "iso",
    "flag",
    "dial"
  ],
  "author": "Vitaliy Balabanov",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.6",
    "typescript": "^4.2.4"
  }
}
